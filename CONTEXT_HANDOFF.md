# CONTEXT_HANDOFF — контекст последней сессии

> Дисциплина: это единственный файл с «сейчас». Обновил состояние — обнови дату ниже. Закончил сессию — допиши итог в «Последний результат» и скорректируй «Дальше». Стабильное знание (числа-гейты, решения, источники) живёт в `docs/xaut-xau-basis-context.md` и сюда не дублируется.
>
> **Состояние на 2026-09-19 ~10:50 UTC (суббота).**

## Где мы

Расхождение держится третьи сутки: тиковый вход ~+30 (10:48), нормы все в плюсе 26–31 (режим перевернулся — 18.09 был в минусе). По минуткам эпизод с 19:07 18.09 длится 919 мин — 3-й по длительности за 24д (растёт; пик 39,9). Книжная позиция с гейтом 30 с открыта с 18.09 11:11 (~47 ч), выходная котировка доходила до 40,7 — живьём популяция «не возвращается за 24ч» (26% по каскадам). Bybit-нога по-прежнему мимо гейта (обе стороны ≥$5k лишь в 15% опросов).

## Сейчас

| Пара (против Lighter XAU) | Спред / норма | Примечание |
|---|---|---|
| Lighter PAXG (тики, вход) | ≈ +30 bp (10:48) | плато третьи сутки, позиция с 18.09 11:11 открыта |
| norm_1h / 3h / 6h / 12h / 24h | +30,5 / +29,8 / +28,5 / +29,0 / +26,4 | все окна в плюсе — режим перевернулся |
| Variational XAUT (минутный рекордер) | ~11 bp | в пределах обычной дневной нормы |

## Инцидент (починен 16.09 12:35 UTC)

`norm_tick` был слеп 15.09 18:32 → 16.09 12:35 (~41,5 ч пустых строк): процесс от 14.09 держал в памяти старую маску `tick_stream_xau_paxg_*.csv`, а дневные файлы удалены при миграции на единый файл. Код в git был уже правильный (коммит 4f9ec20). 1084 пустые строки восстановлены из сердцебиений (ряд непрерывен, n_hb=1425 до/после сходится). Урок: при смене схемы файлов процессы перезапускать сразу.

## Что запущено

- Рекордер: `python3 src/record_spread.py --outdir data`, лог `/tmp/light_spread.log`, пишет в `data/spread_2026-09.csv` каждые 60 с.
- Поток Lighter ticker/92+48: `src/stream_tick_lighter.py --threshold 5 --outdir data`, лог `/tmp/light_stream.log`, файл `data/tick_stream_xau_paxg.csv` (один файл; CRLF-окончания — `grep ',1$'` не работает, читать CSV-парсером).
- Счётчик норм: `src/norm_tick.py --outdir data`, лог `/tmp/light_norm.log`, перезапущен 16.09 12:35, пишет `data/tick_norm_ГГГГ-ММ-ДД.csv`.
- Быстрый кросс-опрос: `src/poll_cross_fast.py --outdir data` (15 с), лог `/tmp/light_crossfast.log`, файл `data/cross_fast_ГГГГ-ММ-ДД.csv` — 87 строк/опрос с 16.09 ~13:55 UTC (10 акций на vari+lighter+aster; SPY пропущен — нет в Variational).
- Кросс-фандинг: `python3 src/record_funding.py --outdir data` (10 мин), лог `/tmp/light_funding.log`, файл `data/funding_YYYY-MM.csv` — XAU/PAXG/XAG/BTC/ETH × lighter/binance/bybit/hyperliquid (BTC/ETH с 18.09, 20 строк/опрос), запущен 17.09 (см. контекст §6 п.12, 15). Бэкфиллы Binance: PAXG с 26.06, BTC/ETH с 05.04 — числа в контексте §6 п.12.
- Книги CEX: `python3 src/poll_cex_books.py --outdir data` (60 с), лог `/tmp/light_cexbooks.log`, файл `data/cex_books_ГГГГ-ММ-ДД.csv` — Binance PAXG-perp + XAUT-спот, Bybit PAXG-perp; запущен 17.09 (см. контекст §6 п.16, 18; раньше CEX не писались по решению «только торгуемые площадки»).

## Последний результат

(Разборы 16.09 и ранее — в `docs/xaut-xau-basis-context.md` §6/§9/§11/§12, сюда не дублируются.)
- 2026-09-17 ~13:10 UTC: прикид направления 2 (кэрри B) — числа, вердикт и калибровка в контексте §6 п.12–13; скрипт `/tmp/opencode/carry.py`. Решение 17.09: шипы не стопить («пересиживать»), стоп — только по удержанию уровня.
- 2026-09-18 ~05:40 UTC (ночной разбор, скрипт `/tmp/opencode/check3.py`): Триггер — 0 эпизодов ≥20 за 16 ч, max|вход| 19,14 (04:04). Фандинг: дивергенция XAU max 4,40 (выброс Bybit 21:15), PAXG max 1,84 — ниже издержек. Связка CEX↔Lighter — в контексте §6 п.18.
- 2026-09-18 ~05:38 UTC (починка наблюдаемости опросника): падения молча глотались — добавлен суффикс `miss=venue:sym[:code]`, ретрай Lighter 3, воркеры 5; правило — только полные циклы. Живой сигнал полноты — только `rows=` в логе (группировка строк CSV по опросам — артефакт).
- 2026-09-18 ~07:30 UTC (батч-миграция опросника): Lighter — 1 вызов без market_id, Aster — 3 вызова, ~36 запросов/цикл (было ~180), цикл ~4 с; пропуски пишутся `""`-строками (граница 18.09 ~07:24 UTC). Перезапущен, 87/87.
- 2026-09-18 ~08:45 UTC (четвёрка из ревизии GLM/Hy4 — вердикты в контексте §6 п.12–17, скрипты в `/tmp/opencode/`): (1) BTC/ETH в рекордере — начисление слабее PAXG, направление 2 закрыто как ядро; (2) каскады (`cascade.py`) — реверс убит, сидение med 3ч, 26% не возвращаются; (3) оракул-лаг (`oracle.py`) — KILL; (4) гейт направления 3 + Bybit KILL-кандидат.
- 2026-09-18 ~15:50 UTC (разбор первого плато Триггера-20; счёт эпизодов — с гистерезисом 20/10, без неё одно плато дробится на тысячи «эпизодов»): вооружённый эпизод один на весь день (игла 06:06, перепостановки ниже 10 не было); внутри него режим плато 11:09–13:2x (80% тиков ≥20, пик 25,59), затухание к 13–14ч, всплеск 14:49, новая волна с 15:46 (~+21). Схлопывание за ~2–3ч совпало с медианой каскадов (t_half med 3ч) — путь медианный, не «удержание уровня».
- 2026-09-18 ~16:00 UTC (dry-run, `/tmp/opencode/paper.py`, переписан с гистерезисом 20/10 — первая версия без неё давала 49 ложных «эпизодов»): правило «выход при возврате ≤5 или таймаут 4ч», заливка со сдвигом L=5/10/20/30/60 с. Итог дня удалён 19.09 — был посчитан на одном незакрытом эпизоде, это не статистика. Заливка возможна при любом L. (Фандинг в симуляции не учтён; отсылка к кэрри была ошибкой.)
- 2026-09-18 ~16:30 UTC (кривая возврата, `/tmp/opencode/revert.py`, входы с гистерезисом, выход ≤5): X=10 — 16 входов, висит>1ч лишь 6%; X=15 — 9 входов, 56%; X=20 — 5 входов, 40%. Вид пороговый, N мал — перепрогон в понедельник.
- 2026-09-19 ~05:50 UTC (ночь пт→сб, все 6 процессов живы): вход не вернулся, а вырос (med 21→31, max 35,78; 100% тиков ≥20 с 17ч). Перепостановки нет — эпизод с 06:06 18.09 висит >23ч (популяция 26% живьём). Кривая возврата без изменений (новых входов нет). Фандинг: XAU div med 0,5 (max 6,8 — вчерашний выброс Bybit), BTC med 0,26→0,96. Bybit ≥$5k обе стороны в 15% опросов — KILL-кандидат стоит. Смотреть: открытие США в понедельник — схлопнется ли базис.
- 2026-09-19 ~07:15 UTC (история расхождений — обе закачки выполнены): Lighter 26.08→19.09 (34 676 минуток на 10:26, скрипт `src/backfill_lighter_1m.py --days 24`): 265 входов ≥20 с гистерезисом (264 закрытых + 1 открытый; ~11/сутки в среднем), med длительность 11 мин, дольше 2ч — 14 шт, рекорд 28.08 — 57ч/пик 57,3. Текущий эпизод — 3-й по длительности (919 мин на 10:26, растёт), по пику во втором десятке: крупный, не беспрецедентный. Binance XAUT/PAXG-спот 26.03→19.09 (4241ч, `/tmp/opencode/binance_year.py`): всего 14 входов за 6 мес (13 из 14 отрицательные), возврат под 10 за med 44ч, до 5 на часовках ни разу. Вердикт — в контексте §6 п.14.
- 2026-09-19 ~08:00 UTC (замер входов/возвратов, `/tmp/opencode/excursion.py`, 264 закрытых входа за 24д): вход med 21,5; уход против med +1,7 (p90 +12,3, max +27,5); возврат-минимум med 8,1; до 5 доходит 16%. Круг по минуткам ≈ +9…+10 bp/вход; винрейт 100% — артефакт правила счёта. Вердикт — в контексте §6 п.20. Одиночные итоги (paper ~+1 bp, фандинг +1,9 bp на незакрытом эпизоде) удалены из выводов 19.09.
- 2026-09-19 ~10:00 UTC (позиционный учёт на книгах, `/tmp/opencode/book_position.py`, одна позиция — один выход): за 14–19.09 закрытых кругов ноль, открыта одна позиция (заливка 18.09 11:11 по 21,3); иголок <30 с — 404 шт (med 0,2 с). Прошлые «3 залитых» были тройным счётом плато — исправлено. Разрыв «минутка−книги» med 11,4 bp. Решение: минутки — только режим/история, деньги — только книги (§11). Вердикт — в контексте §6 п.21.

## Дальше

1. Акции в скане: сбор идёт с 16.09 ~13:55 UTC под гейтами §12 — вердикт ~30.09 (14 суток, книги ≥$5k, X = круг + 5 bp, KILL-правила).
2. Триггер-20 (сторожевой, определение): |вход| ≥ 20 bp (2× круг издержек 3–5 bp) — это детектор, не вход. Гейт сделки рабочей пары (решение 19.09): вход после 30-с забега ≥20 по книгам + заливка 10 с; счёт позиций с перепостановкой на книгах (§6 п.21, §11). Выходы гонки не требуют — по возврату цены в диапазон, неспешно.
3. Открытые вопросы — `docs/xaut-xau-basis-context.md` §9 (вопросы 1, 2, 7 закрыты 16.09).
4. Уикенд-наблюдение 18→22.09: вход висит +20 всю пятницу; в воскресенье 14.09 был −7 весь день. Смотреть: схлопнется ли к понедельнику (возврат к baseline) или дрейф продолжится. Перепрогнать `/tmp/opencode/revert.py` в понедельник (N подрастёт) и тогда зафиксировать срок «возврата уже скорее всего не будет» вместо выдуманных 4ч.
