# CONTEXT_HANDOFF — контекст последней сессии

> Дисциплина: это единственный файл с «сейчас». Обновил состояние — обнови дату ниже. Закончил сессию — допиши итог в «Последний результат» и скорректируй «Дальше». Стабильное знание (числа-гейты, решения, источники) живёт в `docs/xaut-xau-basis-context.md` и сюда не дублируется.
>
> **Состояние на 2026-09-15 18:30 UTC (вторник).**

## Где мы

Входа нет. Золото приподнялось: Variational XAUT +18,5 bp (максимум наблюдения), норма Lighter-пары впервые в плюсе (1ч +2,7). Эпизодов ≥40 bp на исполнимых ногах не было; мигает только тонкий Paradex PAXG (до −100 bp). Его XAU/XAG/XPT — мёртвые книги (bid/ask в разы), из статистики исключены.

## Сейчас

| Пара (против Lighter XAU) | Спред / норма | Примечание |
|---|---|---|
| Lighter PAXG (тики, вход) | ≈ +3 bp | режим ушёл в плюс впервые |
| Lighter PAXG norm_1h / norm_24h | +2,73 / −1,36 bp | лестница едет вверх |
| Variational XAUT (минутный рекордер) | +18,5 bp | максимум за всё наблюдение |

Рабочий гейт — `|спред − norm_1h| > X` (абсолютный `|вход| > 5` выродился: срабатывал в 94% тиков; подробности — контекст §11). `≥40 bp` на исполнимых ногах — ноль за всё время.

## Что запущено

- Рекордер: `python3 src/record_spread.py --outdir data` (из корня репо), лог `/tmp/light_spread.log` (вне репо), пишет в `data/spread_2026-09.csv` каждые 60 с.
- Тиковый опрос Lighter остановлен 10:07 UTC (снят после часа сравнения — поток строго точнее).
- Поток Lighter ticker/92+48: `src/stream_tick_lighter.py --threshold 5 --outdir data`, лог `/tmp/light_stream.log`, файл `data/tick_stream_xau_paxg.csv` (один файл без даты: только |вход|>5 + буфер 5 с + сердце 1/мин).
- Счётчик норм: `src/norm_tick.py --outdir data`, лог `/tmp/light_norm.log`, файл `data/tick_norm_ГГГГ-ММ-ДД.csv` (усечённое среднее P10–P90 сердцебиений за 1/3/6/12/24 ч, раз в минуту).
- Быстрый кросс-опрос: `src/poll_cross_fast.py --outdir data` (15 с, цикл ~5 с после распараллеливания), лог `/tmp/light_crossfast.log`, файл `data/cross_fast_ГГГГ-ММ-ДД.csv` — 57 строк/опрос: Variational (12: XAUT/XAU/PAXG/XAG/XPT/XPD/WTI/NATGAS/BTC/ETH/SOL/1000PEPE) + Lighter (11) + HL (5: PAXG/BTC/ETH/SOL perp + XAUT0 spot) + Aster (11) + Paradex (9, bbo) + ApeX (9). Нет строк: Paradex XPD, ApeX XPT/XPD (контрактов нет). Ostium исключён решением, медь пропущена (одна площадка).
- Бэкфилл Aster: `src/backfill_aster_1m.py`, файлы `data/aster_{xau,paxg,xag,xpt,xpd,wti,natgas}_1m.csv` с 2026-09-07 (~7,6 сут). Paradex/ApeX истории без ключа не отдают (Paradex klines — 401, ApeX — только последние ~200 мин) — только живой опрос.
- Бэкфилл Lighter завершён 2026-09-13 ~14:16 UTC (повторять по мере необходимости).

## Последний результат

- 2026-09-13: тема выделена из SoSimple в `Hohlas/light`; контекст переписан как handoff-док; README/data-README ужаты до указателей; volatile вынесено сюда.
- 2026-09-14: фокус — пара Lighter XAU/PAXG, исполнение ботом ~10 с, только входы (выходы не планируются). Стакан открыт без ключа. Опрос снят в пользу потока (час сравнения). Поток на пороге 5 + счётчик норм 1/3/6/12/24 ч. Эпизоды >5 bp: 16 шт 0–170 с; ≥10 bp — одна иголка 1,6 с (неисполнима). Тактика и схемы файлов зафиксированы: контекст §11, `data/README.md`.
- 2026-09-14 11:25 UTC: три процесса живы (минутный рекордер, поток, нормы). Старшие окна норм прогреются к утру 15.09.
- 2026-09-14 11:30 UTC: контекст прибран (убрано протухшее: 403-ограничение, статусы §6п5/§9, указатели и трафик §4–§5; добавлен §11-акцент в TL;DR).
- 2026-09-14 ~13:40 UTC: запущен быстрый кросс-опрос всех трёх площадок (15 с). HL: XAU/XAUT отсутствуют (есть только PAXG-перп + обёрнутый XAUT0-спот для любопытства). Variational тики не отдаёт (только снимок, обновление ~35–60 с) — дедуп по `updated_at` на стороне анализа.
- 2026-09-15 06:25 UTC: всё живо (4/4, ноль ошибок кроме benign WS-таймаутов потока). Статистика ~17 ч кросса + 7 дн Aster/Lighter + 22 ч тиков — см. чат; коротко: ≥40 bp — ноль везде кроме минут по 2–12 мин/нед внутри Aster/Lighter; тише всех HL PAXG и Aster XAG; Paradex PAXG мигает (тонкий), его XAU/XAG — мёртвые книги.
- 2026-09-15 18:30 UTC: опросник 57 строк (металлы+энерго+контроль+пилот), ротация файла потока починена (перезапуск 18:25 UTC). Режим в плюсе (см. «Где мы»).
- 2026-09-15 ~18:35 UTC: тики переведены на один файл без даты (`tick_stream_xau_paxg.csv`, старые склеены и удалены); маска norm_tick обновлена.

## Дальше

1. Разбор входов `|спред − norm_1h| > X` за сутки тиков Lighter-пары.
2. Вердикт по контролю (BTC/ETH/SOL) и пилоту (1000PEPE) через 1–2 суток сбора.
3. При первом эпизоде ≥40 bp на исполнимой ноге — профиль тиковой длительности (иголка или плато).
4. Открытые вопросы — `docs/xaut-xau-basis-context.md` §9; тактика пары — §11; скан — §12.
