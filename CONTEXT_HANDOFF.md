# CONTEXT_HANDOFF — контекст последней сессии

> Дисциплина: это единственный файл с «сейчас». Обновил состояние — обнови дату ниже. Закончил сессию — допиши итог в «Последний результат» и скорректируй «Дальше». Стабильное знание (числа-гейты, решения, источники) живёт в `docs/xaut-xau-basis-context.md` и сюда не дублируется.
>
> **Состояние на 2026-09-14 11:25 UTC (понедельник).**

## Где мы

Входа нет, идёт тиковый сбор пары Lighter XAU/PAXG. Выходное окно закрылось в 04:00 UTC; режим сползает вниз (часовая норма −6,6 против суточной −4,3). Эпизодов ≥40 bp не было нигде.

## Сейчас

| Пара (против Lighter XAU) | Спред / норма | Примечание |
|---|---|---|
| Lighter PAXG (тики, вход) | ≈ −6,6 bp | дрейф вниз, тихо |
| Lighter PAXG norm_1h / norm_24h | −6,63 / −4,35 bp | лестница рвётся — режим едет |
| Variational XAUT | н/д (минутный рекордер идёт) | фокус смещён на Lighter-пару |

Сигнал `|вход| > 5 bp` срабатывает (рынок около −6…−8 bp); `≥40 bp` — ноль за всё время наблюдений.

## Что запущено

- Рекордер: `python3 src/record_spread.py --outdir data` (из корня репо), лог `/tmp/light_spread.log` (вне репо), пишет в `data/spread_2026-09.csv` каждые 60 с.
- Тиковый опрос Lighter остановлен 10:07 UTC (снят после часа сравнения — поток строго точнее).
- Поток Lighter ticker/92+48: `src/stream_tick_lighter.py --threshold 5 --outdir data`, лог `/tmp/light_stream.log`, файл `data/tick_stream_xau_paxg_ГГГГ-ММ-ДД.csv` (только |вход|>5 + буфер 5 с + сердце 1/мин).
- Счётчик норм: `src/norm_tick.py --outdir data`, лог `/tmp/light_norm.log`, файл `data/tick_norm_ГГГГ-ММ-ДД.csv` (усечённое среднее P10–P90 сердцебиений за 1/3/6/12/24 ч, раз в минуту). Замер 11:16 UTC: norm_1h −6.58, 3h −4.43, 6–24h −4.23 (режим сползает вниз).
- Быстрый кросс-опрос: `src/poll_cross_fast.py --outdir data` (15 с), лог `/tmp/light_crossfast.log`, файл `data/cross_fast_ГГГГ-ММ-ДД.csv` (7 строк/опрос: Variational XAUT/XAU/PAXG + Lighter XAU/PAXG + HL PAXG perp + HL XAUT0 spot для любопытства).
- Бэкфилл Lighter завершён 2026-09-13 ~14:16 UTC (повторять по мере необходимости).

## Последний результат

- 2026-09-13: тема выделена из SoSimple в `Hohlas/light`; контекст переписан как handoff-док; README/data-README ужаты до указателей; volatile вынесено сюда.
- 2026-09-14: фокус — пара Lighter XAU/PAXG, исполнение ботом ~10 с, только входы (выходы не планируются). Стакан открыт без ключа. Опрос снят в пользу потока (час сравнения). Поток на пороге 5 + счётчик норм 1/3/6/12/24 ч. Эпизоды >5 bp: 16 шт 0–170 с; ≥10 bp — одна иголка 1,6 с (неисполнима). Тактика и схемы файлов зафиксированы: контекст §11, `data/README.md`.
- 2026-09-14 11:25 UTC: три процесса живы (минутный рекордер, поток, нормы). Старшие окна норм прогреются к утру 15.09.
- 2026-09-14 11:30 UTC: контекст прибран (убрано протухшее: 403-ограничение, статусы §6п5/§9, указатели и трафик §4–§5; добавлен §11-акцент в TL;DR).
- 2026-09-14 ~13:40 UTC: запущен быстрый кросс-опрос всех трёх площадок (15 с). HL: XAU/XAUT отсутствуют (есть только PAXG-перп + обёрнутый XAUT0-спот для любопытства). Variational тики не отдаёт (только снимок, обновление ~35–60 с) — дедуп по `updated_at` на стороне анализа.

## Дальше

1. Дождаться прогрева норм 12/24 ч (утро 15.09) → разбор наличия входов `|спред − norm_1h| > X` за сутки.
2. При первом эпизоде ≥40 bp — профиль тиковой длительности с буферами ±5 с (иголка или плато).
3. Открытые вопросы — `docs/xaut-xau-basis-context.md` §9; тактика пары — §11.
