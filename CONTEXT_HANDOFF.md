# CONTEXT_HANDOFF — контекст последней сессии

> Дисциплина: это единственный файл с «сейчас». Обновил состояние — обнови дату ниже. Закончил сессию — допиши итог в «Последний результат» и скорректируй «Дальше». Стабильное знание (числа-гейты, решения, источники) живёт в `docs/xaut-xau-basis-context.md` и сюда не дублируется.
>
> **Состояние на 2026-09-18 ~07:30 UTC (пятница).**

## Где мы

Входа нет. Рынок раскалился за ночь: norm_1h +13,5 (было −3,7), тиковый вход +14 bp, vari_xaut 20–31, xag ~20–25, pdax_paxg до 19,6. **Триггер-20 за сутки не сработал ни разу** (0 эпизодов ≥20 с 17.09 13:40), ближайший подход — 19,14 bp в 18.09 04:04 (0,86 bp до порога). Смотреть следующим разбором: разгорится или схлопнется.

## Сейчас

| Пара (против Lighter XAU) | Спред / норма | Примечание |
|---|---|---|
| Lighter PAXG (тики, вход) | ≈ −3 bp | режим сполз в минус после 18:00 |
| norm_1h / 3h / 6h / 12h / 24h | −3,7 / −0,1 / +1,6 / +0,8 / +1,2 | короткие окна ушли в минус, длинные в плюсе |
| Variational XAUT (минутный рекордер) | ~19 bp | в пределах обычной дневной нормы |

## Инцидент (починен 16.09 12:35 UTC)

`norm_tick` был слеп 15.09 18:32 → 16.09 12:35 (~41,5 ч пустых строк): процесс от 14.09 держал в памяти старую маску `tick_stream_xau_paxg_*.csv`, а дневные файлы удалены при миграции на единый файл. Код в git был уже правильный (коммит 4f9ec20). 1084 пустые строки восстановлены из сердцебиений (ряд непрерывен, n_hb=1425 до/после сходится). Урок: при смене схемы файлов процессы перезапускать сразу.

## Что запущено

- Рекордер: `python3 src/record_spread.py --outdir data`, лог `/tmp/light_spread.log`, пишет в `data/spread_2026-09.csv` каждые 60 с.
- Поток Lighter ticker/92+48: `src/stream_tick_lighter.py --threshold 5 --outdir data`, лог `/tmp/light_stream.log`, файл `data/tick_stream_xau_paxg.csv` (один файл; CRLF-окончания — `grep ',1$'` не работает, читать CSV-парсером).
- Счётчик норм: `src/norm_tick.py --outdir data`, лог `/tmp/light_norm.log`, перезапущен 16.09 12:35, пишет `data/tick_norm_ГГГГ-ММ-ДД.csv`.
- Быстрый кросс-опрос: `src/poll_cross_fast.py --outdir data` (15 с), лог `/tmp/light_crossfast.log`, файл `data/cross_fast_ГГГГ-ММ-ДД.csv` — 87 строк/опрос с 16.09 ~13:55 UTC (было 57; +30: 10 акций AAPL/AMZN/COIN/HOOD/META/MSFT/NVDA/QQQ/SPX/TSLA на vari+lighter+aster; SPY пропущен — нет в Variational).
- Бэкфиллы обновлены 16.09 ~12:39: Aster 1m (7 символов × 11 000 баров, по 16.09 12:36), Lighter 1m xau/paxg/xautspot (по 12:39). Paradex/ApeX истории без ключа по-прежнему нет.
- Кросс-фандинг: `python3 src/record_funding.py --outdir data` (10 мин), лог `/tmp/light_funding.log`, файл `data/funding_YYYY-MM.csv` — XAU/PAXG/XAG × lighter/binance/bybit/hyperliquid, запущен 17.09 ~11:35 UTC (направления explorer.md 2–3). Первые числа: дивергенция XAU 0,72 bp / PAXG 0,86 bp (края нет); бэкфилл Binance PAXGUSDT 26.06→17.09 (500 точек, ~4 ч): p50 0,27 / min −0,98 / max 0,50 bp за интервал.
- Книги CEX: `python3 src/poll_cex_books.py --outdir data` (60 с), лог `/tmp/light_cexbooks.log`, файл `data/cex_books_ГГГГ-ММ-ДД.csv` — Binance PAXG-perp + XAUT-спот, Bybit PAXG-perp; запущен 17.09 ~13:38 UTC (направление explorer.md 1; раньше CEX не писались по решению «только торгуемые площадки»). Первые числа: спреды 0,02–0,23 bp, глубина топ-5 Binance ~$25–39k vs Bybit ~$3–4k.

## Последний результат

- 2026-09-16 (разбор 46,4 ч кросса + 51,9 ч тиков; детали — контекст §9/§11/§12):
  - Контроль (BTC/ETH/SOL) тише золота по маркировкам против Lighter (p95 2,3–4,9 против 5,5–20,5 у XAU/PAXG) — шум золота свой, не площадочный. Но на исполнимых кроссах с Variational крипта громче золота: артефакт лага RFQ (котировка ~1/мин, протухлость ~50 с; corr |спред|↔|движение 60 с| = 0,27–0,60; спокойный рынок 2–6 bp, при движении 8–13 bp).
  - Пилот 1000PEPE: вердикт — не сигнал, профиль неотличим от контроля (тот же лаг-артефакт). Хвост крипты не расширять.
  - Дедуп Variational: ровно 75% строк повторы, 60 уникальных котировок/ч на символ (все 12 синхронно), реальная детализация кросс-спредов с ногой vari = 1 наблюдение/мин.
  - Мёртвые книги Paradex: + NATGAS (med −8629 bp), WTI (до −2073), SOL (>10 bp 76% времени); ETH/BTC живые узкие.
  - Тики пары: эпизоды >10 bp — 1815 шт за 51,9 ч, медиана 0,2 с, ≥10 с живут 4,8%, макс 86,9 с (строго) / 136,8 с (допуск гэпов 70 с). Дикий режим −7…−8 bp был только 14–15.09.
  - Живое пересечение листингов (метод §12): новые кандидаты — акции/индексы (QQQ, AAPL, TSLA, NVDA, AMZN, META, MSFT, SPX, HOOD, COIN на vari+lighter+aster; META/MSFT/SPX +para; SPY только 2 площадки — пропуск). Глубина $5k обе стороны и исполнимость подтверждены замером 16.09, снимки кроссов до ±18 bp. Добавлено в скан 16.09 ~13:55 по решению пользователя (см. строку ниже).
- 2026-09-16 ~13:55 UTC: акции в скане (одобрено пользователем, список полностью): +30 строк/опрос (87 всего), все 10 бумаг на 3 площадках с ценами, цикл ~5–6 с, ошибок нет. Часы 14-суточных гейтов запущены — вердикт ~30.09. Кроссы aster↔lighter в лог добавлены (`eq_qqq/aapl/tsla/nvda`).
- 2026-09-16 ~17:50 UTC: ноги Lighter-акций мигали (TSLA 57%, SPX 88%, QQQ 95% за 2 ч; плюс минутный общий отказ Lighter 17:44) — добавлен 1 ретрай в `poll_lighter`, опросник перезапущен, 24/24 циклов полные. Variational/Aster по акциям — 100%.
- 2026-09-16 18:00:04–18:00:14 UTC: ПЕРВОЕ срабатывание Триггера-20: 85 тиков |вход| ≥ 20 bp, из них 12 ≥ 40 bp, пик +43,39 bp (18:00:12). Длительность 10,2 с. Причина — резкое движение золота +0,4% за ~10 с (нога XAU убежала от PAXG; подтверждено маркировками Lighter и стаканом Aster). Классификация отложена до замера времени сделки (все тики в файле). Режим после: вход ≈ −3 bp, norm_1h −3,7.
- 2026-09-17 ~13:10 UTC: прикид направления 2 (кэрри, вариант B: лонг PAXG + шорт XAU-PERP; скрипт `/tmp/opencode/carry.py`, прокси: PAXG-перп вместо спота = нижняя граница риска, close свечей = last-trade): фандинг Binance PAXG 83 дня +108,7 bp (~4,77%/год, + в 398/500 интервалов, худшая серия минусов 10 интервалов); базис Lighter 7 дней mean +5,5 / p95 18,8 / max +48,2 bp (среднее +5,5 — буфер ЗА позицию при схлопывании к нулю, хвост до +48 — против), excursions ≥+10 bp — 27% времени, ≥+20 bp — 4%, самый длинный заход 127 мин; Aster подтверждает форму. Арифметика: кэрри ~1,31 bp/сутки, круг входа-выхода ~8 bp окупается за ~6 дней, шок 18:00 (+43 bp) = ~33 дня кэрри за 10 с. Решение 17.09: тиковые шипы выходом не стопить («пересиживать»), стоп — только по удержанию уровня. Вердикт: кэрри существует, как ядро strategies — нет (доходность не компенсирует риск перп-позиции).
- 2026-09-18 ~05:40 UTC (ночной разбор, скрипт `/tmp/opencode/check3.py`): Триггер — 0 эпизодов ≥20 за 16 ч, max|вход| 19,14 (04:04). Фандинг за 18 ч (109 опросов): дивергенция XAU max 4,40 bp (пик 17.09 21:15 — Bybit 6,9 против 2,5–3,3 у остальных; к утру 1,68, ведут HL 2,84 + Bybit 2,56), PAXG max 1,84 — первая живая динамика, всё ещё ниже издержек. CEX↔Lighter (955 склеенных минут): Binance XAUT-спот vs Lighter XAU — стойкая премия Lighter p50 +8,9 (17.09) / +5,9 (18.09), max +17,9 — структурный оффсет, мертв на розничных спот-комиссиях (10 bp/сторона); Binance-perp ±4 bp, Bybit ±6 bp — шум, книги Bybit <$5k.
- 2026-09-18 ~05:38 UTC (починка наблюдаемости опросника): все 4 многорынковых опросника молча глотали падения (`continue`) — добавлен суффикс лога `miss=venue:sym`, ретрай Lighter 2→3, воркеры Lighter 10→5; опросник перезапущен (первый прогон 87/87). Ложная тревога по файлу: строки CSV штампуются порядочно (не по опросам) — «опросов с 1–20 строками» не существует, это артефакт моей группировки; живой сигнал — только `rows=` в логе. Нога Lighter флэпает (78% опросов частичные, дропы ротируются; прямые пробы OK). Гейтовое правило: в зачёт только полные циклы.
- 2026-09-18 ~07:30 UTC (батч-миграция опросника по гипотезам пользователя — все три приняты): корень ночных флейков — ~180 HTTP-запросов/цикл душили транспорт (рынки живы, прямые пробы 200). Lighter переведён на 1 batch-вызов `orderBookDetails` без market_id (235 рынков, маппинг по market_id), Aster — на 3 batch-вызова без symbol, gzip; Paradex (2,4 МБ сводки с опционами — тяжело) и Apex (пустой ответ без symbol) оставлены поточечно. Итого ~36 запросов/цикл (в 5 раз меньше), цикл 6→4 с, все ноги Lighter из одного снимка (синхронность выросла). Ошибки: HTTP-код + срез тела 120 символов в `miss=venue:sym:code` и `errors=`. Пропуски: вместо `continue` пишутся `""`-строки в том же CSV (пустой `mark` = ноги не было; граница эпох — 2026-09-18T07:24:02Z, зафиксировано в `data/README` + §12; `spreads()` пустые `mark` уже отфильтровывал — формат совместим). Проба 87/87, рестарт, первые 3 опроса 87/87 без miss. `""`-ветка в проде пока не наблюдалась (код-ревью пройдено, живое подтверждение — при первом естественном miss).
- 2026-09-17 ~11:35 UTC: сбор кросс-фандинга запущен немедленно по решению пользователя (совет «один стакан» проигнорирован, порядок из разбора explorer.md принят, п.4 зафиксировано как «уже в работе»): `src/record_funding.py`, 12 строк/опрос (3 символа × 4 площадки), дивергенция на старте XAU 0,72 / PAXG 0,86 bp — края для направления 3 сейчас нет. Ретроспектива Binance PAXG (83 дня): фандинг почти всегда положительный, p50 0,27 bp/4ч (~1,6 bp/сутки) — направление 2 (кэрри) с ходу не мертво, дальше нужен разбор просадок базиса.

## Дальше

1. Акции в скане: сбор идёт с 16.09 ~13:55 UTC под гейтами §12 — вердикт ~30.09 (14 суток, книги ≥$5k, X = круг + 5 bp, KILL-правила).
2. Триггер-20 (сторожевой, определение): |вход| ≥ 20 bp (2× круг издержек 3–5 bp) — это детектор, не вход. Длительности эпизодов пишутся в файлы как есть, разделение «иголка/плато» — только после замера времени сделки бумажным прогоном (сейчас 10 с — допущение, замер отложен). Выходы гонки не требуют — по возврату цены в диапазон, неспешно.
3. Открытые вопросы — `docs/xaut-xau-basis-context.md` §9 (вопросы 1, 2, 7 закрыты 16.09).
