# Сделка «Variational XAUT (Long) — Lighter XAU (Short)»: контекст для продолжения

- **Создан**: 2026-09-12. **Обновлён**: 2026-09-13 14:31 UTC. Репозиторий: `Hohlas/light` (public).
- **Статус**: RESEARCH_ONLY / DIAGNOSTIC_ONLY. Сделок нет, счетов нет — только публичные read-only API. Торговых выводов нет.
- **Как читать**: канонические числа — в §2 (единственное место, где живут доверенные значения). §7 — сжатая история проверки (как мы пришли к этим числам, включая отозванные методы). Всё ниже либо измерено (с источником и временем), либо помечено.
- **Ярлыки**: `[ФАКТ]` — измерено/документация, `[ГИПОТЕЗА]` — правдоподобно, не проверено, `[СПЕКУЛЯЦИЯ]` — догадка.

## 0. TL;DR

1. Это **не** арбитраж одного актива, а базисный трейд **между разными активами**: токен XAUT (Tether Gold) против золота XAU, через два перпетуала. Несёт базисный риск.
2. Норма спреда: **медиана +34…+37 bp** (XAUT дешевле золота). Заявленные автором «0,87%» за последние 62 дня на проверяемых парах **не наблюдались ни разу** (max 74 bp); хвост первой реконструкции оказался артефактом агрегатора (см. §6).
3. **Сейчас входа нет**: спред ≈ +13 bp по mark (≈ +10 bp по исполнимым) — ниже медианы, т.е. «сужение», а не «расширение». Керри отрицательный (−7.45%/год).
4. Действие (если оно есть) живёт либо в самом XAUT, либо в ноге Variational↔Lighter: обще-рыночный базис спокоен (симуляция по часовым убыточна при задержке ≥1 ч; PAXG-пара на одном Lighter за 7 дней ни разу не вышла за 50 bp).
5. Сбор идёт: рекордер пишет снимки Variational+Lighter каждые 60 с; минутки Lighter забэкфиллены; триггер `|s| ≥ 50 bp` молчит. Окно выходного дня открыто до пн 04:00 UTC.

## 1. Что за сделка

> Исходная идея: «Vari XAUT (Long) — Lighter XAU (Short) … на выходных было расширение до 0,87% … с текущих 0,6% можно набирать. Фандинг на расширении пассивный, на сужение насыпает, в спокойное время сужаемся до 0,2%».

- **Variational (Omni)** — perp-DEX с RFQ-моделью (цену даёт OLP — liquidity provider). Публичный read-only API без истории (см. §10).
- **Lighter** — perp-DEX с ордербуком, **taker fee 0%** `[ФАКТ, API]`. Нужные рынки: XAU-PERP (id 92), PAXG-PERP (48), спот XAUT/USDC (2056).
- **XAUT** = Tether Gold (токен, обеспеченный физическим золотом, торгуется 24/7). **XAU** = золото, бенчмарк, на выходных **закрыто** — заявленный механизм расширения.
- Позиция дельта-нейтральна по золоту; прибыль = сужение спреда + фандинг.
- **Кто платит** `[ГИПОТЕЗА]`: тот, кому нужна срочная ончейн-экспозиция на золото, когда традиционный рынок закрыт/тонок, плюс контрагент по фандингу. Строгой «жертвы» нет — это риск-премия за ликвидность и базис.
- **Перпетуал** (для нем-FX читателя): бессрочный фьючерс без экспирации; якорь цены — фандинг, **терминального схождения нет**, базис может жить вечно.
- **Чем сделка НЕ является**: не арбитраж одного инструмента; не риск-фри (XAUT может неделями держать премию/дисконт: выкуп, доверие к эмитенту, регуляторика, конкуренция PAXG); две ноги на двух протоколах → маржа на обеих, ликвидация одной при живой второй = направленная позиция.
- **Базисный риск** (главный): текущая норма +34…+37 bp — это **цена рисков сегодня**, а не «ошибка». Режим смещается, если Tether меняет условия выкупа, случается кризис доверия (прецеденты USDT: депеги 10.2018, 05.2022, 08.2023 — `[ФАКТ]` по USDT, перенос на XAUT — `[ГИПОТЕЗА]`), делистинги/рост спроса.

## 2. Канонические числа

Спред везде: `s = (lighter_XAU − XAUT) / lighter_XAU`, в bp. «+» = XAUT дешевле золота = состояние для входа по логике автора.

| Величина | Значение | Источник |
|---|---|---|
| Норма спреда (медиана) | **+34…+37 bp** | venues-нативный ряд, 62 дня, часовые (ниже) |
| Venues-нативная статистика, n=1500 ч | mean 36.2, med 33.7, P05 11.2, P25 21.4, P75 52.7, P90 59.3, P95 62.3, P99 68.1, min 1.4, **max 74.2** | Binance XAUT-perp klines + Lighter XAU candles, 2026-07-12 → 2026-09-13 `[ФАКТ]` |
| Weekend vs weekday | mean 40.3 / 34.5 — сдвиг есть, но маленький; «эффект выходного дня» не доминирует | там же |
| Уровень +87 bp за 62 дня | **не встречался ни разу** | там же |
| Round-trip издержки | **≈ 10–11 bp** (Variational спред 5.66 bp size_1k + taker 0.05%, Lighter 0%) | API 2026-09-12 |
| Керри long XAUT / short XAU | **−7.45%/год ≈ −0.020%/сутки** (−10.95% платим на Variational + 3.5% получаем на Lighter) | API 2026-09-12 |
| Исполнимый спред сейчас | **≈ +10 bp** (ask Variational 4348.7 против mark Lighter 4353.23) | 2026-09-13 14:31 UTC |
| Контроль XAU vs PAXG (один Lighter, минутки, 7 дней, n=10 434) | med +10.7, P99 +30.9, max +48.2, **эпизодов ≥50 bp — ноль** | нативные свечи API |
| Триггеры (предлагаемые, не проверенные) | наблюдать `s ≥ +70`, набирать `s ≥ +87`, выход при возврате к `+40…+50` | предрегистрация до первого входа обязательна |

**Симуляция с задержкой исполнения** `[ФАКТ, расчёт 2026-09-12]`: вход при `s_t ≥ порог`, исполнение по ценам часа `t+lag`, выход при `s ≤ 35 bp` или через 48 ч, издержки 12 bp:

| Вход | lag 0 ч (нереалистично) | lag 1 ч | lag 2 ч |
|---|---|---|---|
| ≥60 bp | n=11, +22.3 bp, PF 1.74 | n=11, **−38.1 bp, PF 0.54** | −47.8 bp, PF 0.45 |
| ≥65 bp | n=5, +24.3 bp, PF 3.11 | n=5, +9.7 bp, PF 1.59 | −6.1 bp, PF 0.74 |
| ≥70 bp | n=2, +35.2 bp | n=2, +26.1 bp | n=2, +10.4 bp |

Выводы: весь «край» — в первом часе (нужна минутная автоматизация); даже лучший случай ≈ +24 bp за 62 дня на номинал; 5–11 сделок за 62 дня → `N ≥ 100` достижимо за **1–2 года**. Заявленные автором «0,6% за вход» с этими числами не сходятся — либо исполнение за минуты, либо край в ноге Variational↔Lighter, а не в обще-рыночном базисе.

## 3. Текущее состояние (2026-09-13 14:31 UTC, воскресенье)

| Пара (против Lighter XAU) | Спред | Комментарий |
|---|---|---|
| Variational XAUT | ≈ +13 bp mark / **≈ +10 bp исполнимый** | ниже медианы — входа нет |
| Lighter PAXG | ≈ +0.7 bp | тихо |
| Lighter XAUT-spot | единицы bp, знак плавает | принты мёртвого рынка, незначимо (см. §6) |

Триггер `|s| ≥ 50 bp` не срабатывает ни на одной паре. Окно выходного дня (пт 20:00 → пн 04:00 UTC) открыто ещё ~13.5 ч — расширение ещё может появиться; рекордер его поймает.

## 4. Данные: что есть и как пополняется

Форматы и provenance — в `data/README.md` (включая честную оговорку о потерянном срезе 12.09 — читать обязательно).

| Файл в `data/` | Содержимое | Статус на 2026-09-13 14:31 UTC |
|---|---|---|
| `spread_YYYY-MM.csv` | снимки рекордера (venue/kind/symbol/mark/bid/ask/funding/OI), append | пишется с 14:09 UTC, каждые 60 с |
| `spread_2026-09_salvaged_log.csv` | 329 минутных значений 3 спредов 12.09 19:52 → 13.09 14:04, восстановлены из stdout-лога (только агрегаты) | спасённый срез, вне гита |
| `spread_sample.csv` | первые 200 сырых строк (12.09 19:51 → 13.09 08:36) | в гите (семпл) |
| `lighter_{xau,paxg,xautspot}_1m.csv` | нативные 1-минутные свечи Lighter (ts_utc,open,high,low,close,vol) | забэкфиллены: xau n=10 415 с 2026-09-06 08:40, paxg/xautspot n=11 000 с 2026-09-05 22:55; вне гита |
| `lighter_xau_1m_sample.csv` | семпл минуток XAU | в гите (семпл) |

Ключевые факты для анализа:
- **У Variational нет истории в принципе** — его «минутки» существуют только как наши 60-секундные снимки (один опрос в минуту = close минутного бара). Другого источника нет; котировки кешируются сервером до 600 с.
- Короткая нога оценивается по mark Lighter (публичного bid/ask нет — book-эндпоинты → 403) `[ФАКТ-ограничение, half-spread ~1 bp — ГИПОТЕЗА]`.
- Сырые bid/ask/funding/OI за 12.09 19:51 → 13.09 14:04 **потеряны безвозвратно** при переносе из SoSimple (см. `data/README.md`); ряд спредов за тот же интервал уцелел.

## 5. Как работать с репозиторием

```
light/
├── README.md            # вход: предмет, статус, запуск
├── requirements.txt     # только стандартная библиотека (Python ≥ 3.10)
├── src/
│   ├── record_spread.py       # рекордер: опрос 60 с → data/spread_YYYY-MM.csv
│   └── backfill_lighter_1m.py # бэкфилл свечей Lighter
├── docs/
│   └── xaut-xau-basis-context.md  # этот файл
└── data/                # рабочие данные (вне гита, кроме семплов и README)
```

```bash
python3 src/record_spread.py --once --outdir data   # разовый снимок
python3 src/record_spread.py --outdir data           # непрерывная запись
python3 src/backfill_lighter_1m.py --days 7 --outdir data
```

Операционка: рекордер запущен 2026-09-13 14:09 UTC (`python3 src/record_spread.py --outdir data`, лог `/tmp/light_spread.log` — вне репо). Флаги в логе: `win` (в окне: выходные пт 20:00 → пн 04:00 UTC + ежедневно 11:00–16:00 UTC), `trig` (`|s| ≥ 50 bp` на любой из трёх пар). Трафик ~100 МБ/сутки (доминирует Variational, 65 КБ gzip/опрос). Git: `main`, remote `git@github.com:Hohlas/light.git` (SSH). Происхождение: выделено 2026-09-13 из `Hohlas/SoSimple` (общий разбор 15 направлений остался там: `docs/superpowers/specs/2026-09-12-arbitrage-directions.md`).

## 6. Снятые варианты и отозванные методы (журнал решений)

1. **Lighter XAUT-spot как нога — СНЯТ** (неисполним *и* неизмерим). Спот мёртв: 5 сделок/сутки (~$2046), нет bid/ask/глубины. Доказательство: во всех 430 минутах со «спредом» ≥50 bp объём спота был **ровно 0** (430/430) — «дислокации» это движение золота против замороженного принта. Свечи спота хранить можно, использовать как цену — нельзя.
2. **CoinGecko XAUT как источник истории — ОТОЗВАН.** Уровни коррелируют с биржами на 0.9988, но часовые доходности — на −0.03 (≈0): в каждый из топ-10 «пиков» Binance и Lighter были согласованы (20–67 bp), а CoinGecko печатал на 1–1.5% ниже. Вся первая реконструкция (медиана 37.1, «64 эпизода ≥87 bp», OU half-life 0.6 ч, кластеризация 11:00–15:00 UTC) — **артефакт, для решений не использовать**. Не повторять этот путь.
3. **CEX-опросы в рекордере — УБРАНЫ.** Binance/Bybit/Bitget/BingX/Hyperliquid служили только ретроспективе и разовому снимку 12.09 (расхождение оказалось обще-рыночным, не venue-специфичным). Решения — только по котировкам торговых площадок.
4. **Зеркальная сделка (вход на узком) — ОТКЛОНЕНА без отдельной предрегистрации**: прибыль ограничена (~20 bp до медианы), убыток нет (наблюдалось −150 bp), платится базис вместо его изъятия; керри +7.45%/год хвостовой риск не покрывает.
5. **Живой вариант (не проверен)**: всё на одной площадке — Variational XAU vs XAUT (кросс-маржа?) или Lighter XAU vs PAXG (теснее и спокойнее: медиана ~10 bp). Снимает логистику двух марж ценой концентрационного риска.

## 7. Как мы сюда пришли (сжатая история)

- 12.09: исходная идея → живой снимок (спред +17.6 bp, керри −7.45%) → часовая реконструкция на CoinGecko (медиана 37 bp, «64 эпизода») → 9 вопросов → venues-нативная перепроверка → CoinGecko отозван, опорный ряд — Binance+Lighter (max 74 bp, +87 bp ни разу) → симуляция (край только в первом часе) → триггерный сбор.
- 13.09: минутки обеих ног (Lighter — нативные, Variational — только снимки) → PAXG-контроль (ноль эпизодов) → спот-нога снята (430/430) → упрощение рекордера до торговых площадок → перенос темы из SoSimple в `Hohlas/light` (с потерей сырого среза 12.09 — см. §4).

## 8. Критерии продолжения / KILL (предрегистрированы)

- 8 недель сбора без эпизода ≥87 bp на паре Variational↔Lighter → гипотеза не подтверждается → KILL/смена инструмента.
- Эпизоды есть, но медианная длительность < 5–10 минут при нашем измеренном лаге исполнения → неисполнима нашей инфраструктурой → KILL (уточнение правила «<30 мин»: неисполнима вручную и без доказательства; код укладывается в минуты, проблема — увидеть дислокацию на минутном опросе, риск частичного заполнения двух ног, adverse selection, автономность в выходные).
- N ≥ 100 эпизодов + гейты (PF ≥ 1.3 при BS_p05 > 1.0, ≥30 на сторону, стресс издержек 2x) → только тогда отдельная предрегистрация сделки. Торговых выводов до этого нет.

## 9. Открытые вопросы

1. Реальная длительность эпизодов на синхронных минутных данных Variational↔Lighter (часовые снимки длительность занижают) — главный неизвестный параметр; ответ даст рекордер при первом же эпизоде.
2. Исполнимая цена Variational при размере 10k–100k (в API есть `size_1k/size_100k/size_1m` — брать из ответа, не считать по mark).
3. Одно-площадочные варианты (§6 п.5): измерить их спреды/фандинг так же строго, прежде чем считать запасными.
4. Точные комиссии/фандинг каждой площадки сверять по API перед любым расчётом PnL (значения на 12.09: Variational taker 0.05% по DefiLlama — перепроверить).
5. Дневной масштаб CoinGecko — проверить отдельно, прежде чем использовать хоть где-то (часовой отозван).

## 10. Источники и грабли API

**Торговые площадки (пишутся рекордером):**
- Variational `https://omni-client-api.prod.ap-northeast-1.variational.io/metadata/stats` — только снапшот (281 КБ, gzip 65 КБ), `/candles`/`/history` → 404, фильтра по тикеру нет, котировки кешируются до 600 с. Docs: `docs.variational.io/technical-documentation/api`, `.../omni/trading/funding-rates` (interest 0.00125%/ч = 10.95%/год), `.../omni/trading/rwa-perpetuals`.
- Lighter `mainnet.zklighter.elliot.ai/api/v1/{orderBooks,orderBookDetails,funding-rates,fundings,candles}` — book-эндпоинты с market_id для стакана → 403 (bid/ask книги недоступны); свечи 1m — до ~2 недель (пагинация `count_back=1000`, бывают транзиентные 400 — нужен retry). Docs: `docs.lighter.xyz/trading/funding` (платежи каждый час), `docs.lighter.xyz/trading/api`.

**Только ретроспектива (в рекордере нет):** Binance `fapi.binance.com/fapi/v1/klines?symbol=XAUTUSDT&interval=1h`; разовый снимок 12.09 — Bybit/Bitget/BingX/Hyperliquid(PAXG).

**Отозван:** CoinGecko `tether-gold/market_chart` на часовом масштабе (§6 п.2).

**Вторичные:** DefiLlama (`rwa/perps/venue/variational`, `contract/variational:xaut`); loris.tools — 17 веню с XAUT-перпом, история фандинга только по API-ключу.

**Не подтверждено:** OKX XAUT-SWAP (инструмента нет, 51001), MEXC (404), Ostium (DNS), Jupiter (формат ответа).
